- Data Structure of:
-- Char
-- Offset (ms)

- OR Data structure of:
-- Time segments (100ms, for example)
--- { char: number of times expected }

- OR Array. Each index represents frame (16ms)
-- Sub array of chars expected.

- Each frame
